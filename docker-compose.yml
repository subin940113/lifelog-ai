services:
  postgres:
    image: postgres:16
    container_name: lifelog-postgres
    environment:
      POSTGRES_DB: lifelog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - lifelog_pg:/var/lib/postgresql/data

volumes:
  lifelog_pg: